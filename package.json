{
  "name": "workshop-graphql-rappa",
  "devDependencies": {
    "prettier": "^3.5.0"
  },
  "packageManager": "bun@1.2.2",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
